# Backend
AUTORE_BIND_HOST=0.0.0.0
AUTORE_BIND_PORT=5555
# Recommend keeping work dir inside the repo (gitignored)
AUTORE_WORK_DIR=/path/to/this/repo/work

# Ghidra
# Path to Ghidra's analyzeHeadless
# If you used install_ubuntu.sh, it will set this automatically.
GHIDRA_ANALYZE_HEADLESS=/path/to/ghidra/support/analyzeHeadless
GHIDRA_SCRIPTS_DIR=/path/to/this/repo/ghidra_scripts

# Worker (Hybrid AI)
# You can use Anthropic OR an OpenAI-compatible endpoint (or both).

## Anthropic
ANTHROPIC_API_KEY=
# Defaults used when the UI Model field is empty
ANTHROPIC_MODEL_DEFAULT=claude-sonnet-4-5
ANTHROPIC_MODEL_HEAVY=claude-opus-4-5

## OpenAI-compatible (internal/vLLM)
# Base URL can be either:
# - http://host:port
# - http://host:port/v1
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL_DEFAULT=gpt-oss-120b

## Guardrail (worker retry loop)
AUTORE_GUARDRAIL_MAX_ATTEMPTS=4
AUTORE_GUARDRAIL_MIN_CONFIDENCE=0.55
